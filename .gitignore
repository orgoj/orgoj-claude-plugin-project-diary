# Zig cache artifacts (but keep platform binaries in zig-out/bin/)
zig-cache/
.zig-cache/

# Zig build artifacts (except platform binaries for marketplace)
zig-out/*
!zig-out/bin/
zig-out/bin/*
!zig-out/bin/linux-x64/
!zig-out/bin/linux-arm64/
!zig-out/bin/darwin-x64/
!zig-out/bin/darwin-arm64/
!zig-out/bin/windows-x64/
!zig-out/bin/**/*.exe
!zig-out/bin/**/mopc
# Ignore dev build (not platform-specific)
zig-out/bin/mopc
zig-out/bin/mopc.exe

# Allow platform-specific binaries to be committed for marketplace distribution
# (Users won't have Zig installed)

# Test artifacts
/tmp/zig-test/
/tmp/mopc-test-*
tests/fixtures/

# Temporary platform-specific symlinks (created by SessionStart hook)
hooks/mopc
hooks/mopc.exe
bin/mopc
bin/mopc.exe
